[project]
name = "true-love-common"
version = "0.1.1"
description = "True Love Common — 各服务共享的协议定义"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/true_love_common"]
